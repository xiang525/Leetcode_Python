### Problem:
Write a function to find the longest common prefix string amongst an array of strings.

### Hints:
Assumptions:  
1. no spaces;  
2. two functions like `min` and `max` of string list will retune the minimum and maximum string;
3. instead of comparing all strings, compare min and max instead;


#### References:

#### Tags:
String
